name: SSL/TLS Monitor 
on:
  workflow_dispatch

jobs:
  monitor:
    name: SSL/TLS Monitor
    runs-on: ubuntu-latest
    strategy:
      matrix:
        domain:
          - fullprofile.com.au
          - app.waypath.io
          - agridigital.io
          - login.fullprofile.net
          - crm.crm16.fullprofile.com.au
          - app.agridigital.io
          - api.agridigital.io
          - ous.agridigital.io
          - api.waypath.io/ous/swagger/index.html

    steps:
      - name: Monitor - ${{ matrix.domain }}
        uses: bluenovaio/action-tls-monitor@main
        with: 
          domain: ${{ matrix.domain }}
          expiration_days: 600
          approved_protocols: TLSv1.2,TLSv1.3
          alert_method: slack
          alert_token: ${{ secrets.NOTIFICATION_SLACK_WEBHOOK_URL }}

